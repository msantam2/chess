require './player'

class ComputerPlayer < Player
  def get_move(display)
    # computer does not use cursorable to get_move, will evaluate moves and avoid check/checkmate, induce opponent into check/checkmante, or finally just choose a random position
    
  end

  def moves

  end
end
